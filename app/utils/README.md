# Utilities (`app/utils/`)

**Purpose:**
Stateless helper functions and pure logic that can be used anywhere.

**Rules:**
- No database calls.
- No external API calls.
- Examples: String formatting functions, custom date parsers, generic math calculations, or Pandas DataFrame data cleaning utilities that don't rely on app state.